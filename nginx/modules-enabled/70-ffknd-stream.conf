# Ubuntu's libnginx-mod-stream package is loaded by 50-mod-stream.conf.
# Keep this file sorted after it so the stream context is parsed afterwards.
stream {
    include /etc/nginx/stream-conf.d/*.conf;
}
